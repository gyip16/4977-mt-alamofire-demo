# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'alamofire-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for alamofire-demo
  pod 'Alamofire', '~> 4.5'

  target 'alamofire-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'alamofire-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
